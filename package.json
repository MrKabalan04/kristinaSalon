{
  "name": "salon-admin",
  "version": "1.0.0",
  "description": "Salon services admin panel",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongodb": "^5.7.0",
    "firebase": "^10.7.0"
  }
}
